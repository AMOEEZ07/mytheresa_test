In order to execute the current project here are thing you should know:
1- This project is selenium based written in Java using Maven.
2- This project contains two methods as per mentioned in test case 1
3- I have tried to attempt all of three cases, and this architecture I have made supports all three cases but I only managed to finish first test case.
4- For the 3rd use case, I have added the class for excel, called dataReader and Write
5- In order to run this project in different browser or different URL please update the config file accordingly.
6- Update the maven and execute the project
7- Browsers drivers are available in Resources folder

Flow:

Initiate Driver from Driver Class, both classes are extended from TestBase which triggers the Driver class based on configs
Sequence of methods are available in Batch file.
